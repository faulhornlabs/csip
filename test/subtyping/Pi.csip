# elab

import Builtins

data Int : Ty where

constructor builtin F : (Code Int -> Code Int -> Code Int) -> Code Int

constructor builtin G : Code Int -> Code (Int -> Int)

-- F (\x y -> App (G x) y)
F G
